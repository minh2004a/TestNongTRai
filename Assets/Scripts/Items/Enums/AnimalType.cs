namespace TinyFarm.Items
{
    public enum AnimalType
    {
        Chicken,    // Gà (cho trứng)
        Cow,        // Bò (cho sữa)
        Sheep,      // Cừu (cho lông)
        Pig,        // Lợn (cho thịt)
        Goat,       // Dê (cho sữa dê)
        Duck        // Vịt (cho trứng vịt)
    }
}

